import { Injectable } from '@nestjs/common';

/**
 * BetService — placeholder conservé pour les futures opérations sur les bets.
 * La création des bets USER est gérée par BetSlipService.create().
 */
@Injectable()
export class BetService {}
